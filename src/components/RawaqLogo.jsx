// Brand mark — the generated Rawaq app icon (glass arch + plant).
export default function RawaqLogo({ size = 64 }) {
  return (
    <img
      src="/icon-512.png"
      alt="شعار رواق"
      width={size}
      height={size}
      style={{ borderRadius: Math.round(size * 0.24), display: "block" }}
    />
  );
}
