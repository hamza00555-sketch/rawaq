# تفعيل إشعارات «الجهاز مقفل» 🔔

كتبتُ كل الكود اللازم (Service Worker + FCM + الدالة السحابية). باقي عليك
٣ خطوات لمرة واحدة فقط في حساب Firebase، وبعدها الإشعارات توصل للأم حتى
والتطبيق مغلق تماماً.

> **قبل التفعيل:** التطبيق يشتغل عادي بدون هذه الخطوات — بس الإشعارات تظل
> «داخل التطبيق» فقط (لما يكون مفتوح أو بالخلفية). ما ينكسر شي إذا أجّلتها.

---

## 1) فعّل خطة Blaze

- افتح: <https://console.firebase.google.com/project/rawaq-fad4a/usage/details>
- اضغط **Upgrade / ترقية** واختر **Blaze (الدفع حسب الاستخدام)** وأضف بطاقة.
- **مهم — حد الميزانية:** بعد الترقية اضبط تنبيه/سقف ميزانية بسيط (مثلاً 1 دولار)
  من: <https://console.cloud.google.com/billing/budgets> — يضمن ما تتفاجأ بأي مبلغ.
  (استخدامك المتوقع = 0 ريال شهرياً؛ الطبقة المجانية تغطيك بمليون تشغيل).

## 2) ولّد مفتاح Web Push (VAPID) والصقه

- افتح: <https://console.firebase.google.com/project/rawaq-fad4a/settings/cloudmessaging>
- تحت **Web Push certificates** اضغط **Generate key pair**.
- انسخ المفتاح الظاهر (يبدأ بحروف طويلة).
- الصقه في الملف `src/firebase-config.js` مكان `PASTE_VAPID_KEY`:
  ```js
  export const vapidKey = "الصق_المفتاح_هنا";
  ```
- احفظ وارفع التغيير (`git push`) — Vercel ينشر التحديث تلقائياً.

## 3) انشر الدالة السحابية والقواعد

من جهازك (يحتاج Node مثبّت)، شغّل مرة وحدة:

```bash
npm install -g firebase-tools          # مرة وحدة فقط
firebase login                         # سجّل بنفس حساب قوقل مالك المشروع
cd rawaq                               # مجلد المشروع
firebase deploy --only functions,firestore:rules
```

أول نشر للدوال قد يطلب تفعيل بعض الـ APIs — وافق على كل شي يطلبه.

---

## على الآيفون 📱 (مهم)

الآيفون يستقبل هذا النوع من الإشعارات **فقط** إذا كان التطبيق مضاف للشاشة
الرئيسية:

1. افتح <https://rawaq-five.vercel.app> في Safari.
2. زر المشاركة ⬆️ ← **إضافة إلى الشاشة الرئيسية**.
3. افتح التطبيق من الأيقونة الجديدة، ادخل **الإعدادات ← التنبيهات ← تشغيل**،
   ووافق على السماح بالإشعارات.

الأندرويد (جوال خالتك أسما) ما يحتاج هالخطوة — يكفي تشغيل التنبيهات والسماح.

---

## كيف يشتغل باختصار

- جهاز الأم يسجّل «رمز جهاز» (FCM token) ويحفظه في `pushTokens/<رمز المشاركة>`.
- لما العاملة تعلّم مهمة كمنجزة، تتحدّث وثيقة `shares/<id>`.
- الدالة `notifyOnTaskDone` تنتبه للتغيير، تقرأ رمز جهاز الأم، وترسل الإشعار
  عبر FCM — فيوصل حتى والتطبيق مغلق.
- كل نصوص الإشعار محفوظة من جهة التطبيق (بلغة الأم)، فالدالة لا تحتاج تعرف اللغة.

**الكلفة الفعلية المتوقعة: صفر** — ضمن الطبقة المجانية، ومحمي بحد الميزانية.
